#ifndef COMMON_HPP
#define COMMON_HPP

#include <cstdlib>
#include <cassert>

#define UINT8_COUNT (UINT8_MAX + 1)
#define UNREACHABLE() printf("Unreachable!\n"); assert(false);
#define DEBUG true

#endif